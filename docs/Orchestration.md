Subpolar has these built in separation layers:
- Agents: Agents are profiles of capablities including tools and skills the model can access
- Projects: Projects are directory-bound work. Agent sessions/instances work in that project directory.
- General chat: each session has its own workspace directory. This should especially be used for sessions that do not need to access files.